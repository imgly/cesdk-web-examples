import designMode from './design.js';
import photoMode from './photo.js';
import videoMode from './video.js';

export const MODE_OPTIONS = [designMode, videoMode, photoMode];

export { designMode, videoMode, photoMode };
